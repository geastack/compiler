'use strict'
function build (data) {
  let v = null
  v = v.concat([2])
  v = v.concat([2])
  v = v.concat([2])
  v = v.concat([2])
  v = v.concat([2])
  v = v.concat([2])
  v = v.concat([2])
  v = v.concat([2])
  v = v.concat([2])
  v = v.concat([2])
  v = v.concat([2])
  v = v.concat([2])
  v = v.concat([2])
  return v
}
module.exports = build
